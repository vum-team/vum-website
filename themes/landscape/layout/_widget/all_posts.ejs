<% if (site.posts.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title">VUM</h3>
    <div class="widget">
      <ul>
        <% site.posts.sort('order').each(function(post){ %>
          <li>
            <a href="<%- url_for(post.path) %>"><%= post.title || '(no title)' %></a>
          </li>
        <% }) %>
      </ul>
    </div>
  </div>
<% } %>
